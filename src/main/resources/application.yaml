server:
  port: 8080
spring:
  application:
    name: hmdp
  rabbitmq:
      host: 172.16.98.128
      port: 5672
      username: hongze
      password: 1995
  datasource:
    url: jdbc:mysql://172.16.98.128:3306/hmdp?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123
  redis:
    host: 172.16.98.128
    port: 6379
    # password:
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 1
        time-between-eviction-runs: 10s
  jackson:
    default-property-inclusion: non_null # JSON处理时忽略非空字段
mybatis-plus:
  type-aliases-package: com.hmdp.entity # 别名扫描包
logging:
  level:
    org.springframework.amqp: DEBUG
    org.springframework.rabbit: DEBUG
    com.rabbitmq: DEBUG
jwt:
  secret: ${JWT_SECRET:replace-with-your-very-long-secret-key-at-least-32-bytes!!!}

